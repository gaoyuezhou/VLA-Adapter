type: patch_policy
dataset_class: libero.LiberoGoalDataset
data_directory: "${env_vars.datasets.libero}"
language_instruction: null  # null = extract from LIBERO task directory name

# Robot constants
action_dim: 7
num_actions_chunk: 8
proprio_dim: 0
normalization_type: "bounds_q99"
num_views: 2

# Data slicing
window_size: 8
train_fraction: 0.95
subset_fraction: null